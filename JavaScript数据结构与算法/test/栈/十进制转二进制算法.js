// 模块化语法
// import {Stack} from "./基于数组的栈"

/*function convert(number) {
    const s = new Stack()
    let rem
    while (number % 2 !== 0) {
        rem = number - (number % 2) * 2
        s.push(rem)
        number = number % 2
    }
    while (!s.isEmpty()) {
        console.log(s.pop())
    }
}

convert(10)*/
